Open Closed Principle

Definition: Software entities should be open for extension, but closed for modification.

Example:
Using a product list filtering example with the filtering functionality following the OCP